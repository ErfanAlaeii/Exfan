<Window x:Class="ExcelCreator.UI.Dialogs.SearchRowsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title=""
        Height="320"
        Width="440"
        MinHeight="260"
        MinWidth="360"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        FlowDirection="LeftToRight"
        Language="fa-IR"
        Background="{StaticResource SurfaceBrush}"
        Icon="{x:Null}">
    <Border Margin="20"
            Background="White"
            CornerRadius="12"
            BorderBrush="#E2E8F0"
            BorderThickness="1"
            Padding="24"
            FlowDirection="RightToLeft">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="PromptText"
                       Grid.Row="0"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource TextBrush}"
                       TextAlignment="Right"
                       TextWrapping="Wrap"
                       Margin="0,0,0,16"/>

            <StackPanel Grid.Row="1" Margin="0,0,0,12">
                <TextBlock x:Name="FieldLabel"
                           FontWeight="SemiBold"
                           Margin="0,0,0,6"
                           TextAlignment="Right"/>
                <ComboBox x:Name="FieldBox"
                          Padding="10,8"
                          FontSize="14"
                          FlowDirection="RightToLeft"
                          SelectionChanged="FieldBox_SelectionChanged"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,0,0,12">
                <TextBlock x:Name="ValueLabel"
                           FontWeight="SemiBold"
                           Margin="0,0,0,6"
                           TextAlignment="Right"/>
                <ContentControl x:Name="ValueInputHost"/>
            </StackPanel>

            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Left"
                        FlowDirection="RightToLeft"
                        Margin="0,8,0,0">
                <Button x:Name="ConfirmButton"
                        Style="{StaticResource PrimaryButton}"
                        MinWidth="100"
                        Margin="0,0,12,0"
                        IsDefault="True"
                        Click="Confirm_Click"/>
                <Button x:Name="CancelButton"
                        Style="{StaticResource SecondaryButton}"
                        MinWidth="100"
                        IsCancel="True"
                        Click="Cancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
